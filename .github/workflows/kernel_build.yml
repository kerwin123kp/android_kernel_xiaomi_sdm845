name: Build Kernel for Xiaomi SDM845 (LineageOS 22.1)

on:
  push:
    branches:
      - main  # Trigger workflow on push to the main branch
  workflow_dispatch:  # Allow manual triggering from GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the kernel repository
      - name: Checkout kernel repository
        uses: actions/checkout@v2
        with:
          repository: kerwin123kp/android_kernel_xiaomi_sdm845  # Kernel repo
          ref: lineage-22.1  # Branch for LineageOS 22.1

      # Step 2: Set up cross-compilation toolchain
      - name: Set up cross-compilation toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-arm-linux-gnueabi gcc-arm-linux-gnueabihf \
          gcc-aarch64-linux-gnu g++-aarch64-linux-gnu make bc bison flex \
          libssl-dev libelf-dev libncurses5-dev wget

      # Step 3: Download or move the .config file (perseus_config)
      - name: Move or Download .config file
        run: |
          # If you have the perseus_config in the repository already:
          mv path_to_your_config/perseus_config linux/.config

          # OR if you need to download the perseus_config (e.g., hosted externally)
          # curl -L -o linux/.config https://yourserver.com/path/to/perseus_config

      # Step 4: Apply the defconfig (perseus_defconfig)
      - name: Apply defconfig (perseus_defconfig)
        run: |
          cd linux
          make ARCH=arm64 -j$(nproc) perseus_defconfig  # Apply your default config

      # Step 5: Build the kernel
      - name: Build the kernel
        run: |
          cd linux
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- -j$(nproc)

      # Step 6: Upload the kernel image as an artifact
      - name: Upload Kernel Artifact
        uses: actions/upload-artifact@v3  # Updated to v3
        with:
          name: kernel-image
          path: linux/arch/arm64/boot/Image  # Path to the built kernel image
